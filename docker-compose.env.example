# MindVault Docker environment variables (example).
# Copy to .env for `docker compose`. Never commit real secrets.
MV_HOME=/data
MV_HOST=0.0.0.0
MV_PORT=8000
MV_OLLAMA_URL=http://ollama:11434
MV_EMBEDDING_PROVIDER=hash
