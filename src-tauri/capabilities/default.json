{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the MindVault window.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
